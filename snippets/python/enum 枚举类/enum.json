{
  "引入 enum 枚举类": {
    "prefix": ["import_enum_Enum", "im_enum_Enum"],
    "body": ["from enum import Enum  # * 引入 enum 枚举类"],
    "description": ["引入 enum 枚举类"]
  },
  "引入 enum 枚举类.常用模块": {
    "prefix": ["import_enum=", "im_enum="],
    "body": [
      "from enum import Enum, IntEnum, StrEnum, auto  # * 引入 enum 枚举类"
    ],
    "description": ["引入 enum 枚举类.常用模块"]
  },
  "enum.object 创建枚举类对象.tmpl": {
    "prefix": ["create_class_enum=", "c_class_enum="],
    "body": [
      "class ${1:EnumObject}(Enum):",
      "    # * NAME = value: int | str | ...",
      "    $0",
      "    ...",
      ""
    ],
    "description": ["enum.object 创建枚举类对象.tmpl"]
  }
}
