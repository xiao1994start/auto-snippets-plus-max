{
  "pip.依赖名 依赖描述": {
    "prefix": ["pip 依赖名"],
    "body": [
      "pip install -U 依赖名 -i https://mirrors.aliyun.com/pypi/simple/",
      ""
    ],
    "description": ["依赖名 依赖描述"]
  },
  "uv.依赖名 依赖描述": {
    "prefix": ["uv 依赖名"],
    "body": [
      "uv add \"依赖名\" --upgrade --index https://mirrors.aliyun.com/pypi/simple/",
      ""
    ],
    "description": ["依赖名 依赖描述"]
  }
}
