{
  "pip.httpx 网络请求库": {
    "prefix": "pip -httpx",
    "body": [
      "pip install -U httpx -i https://mirrors.aliyun.com/pypi/simple/",
      "pip install -U httpx[http2] -i https://mirrors.aliyun.com/pypi/simple/",
      "pip install -U httpx[brotli] -i https://mirrors.aliyun.com/pypi/simple/",
      "pip install -U httpx[socks] -i https://mirrors.aliyun.com/pypi/simple/",
      ""
    ],
    "description": ["pip -httpx 网络请求库"]
  }
}
