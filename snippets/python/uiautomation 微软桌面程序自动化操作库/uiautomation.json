{
  "引入 uiautomation Windows桌面程序自动化控制依赖": {
    "prefix": "im -uiautomation",
    "body": [
      "from uiautomation import MenuControl, WindowControl  # Windows桌面程序自动化控制依赖",
      "# MenuControl: 菜单控件; WindowControl: 窗口控件"
    ],
    "description": ["引入 uiautomation Windows桌面程序自动化控制依赖"]
  },
  "uiautomation.WindowControl 实例化绑定窗口": {
    "prefix": ["uiautomation.WindowControl -uiautomation"],
    "body": [
      "WindowControl(searchDepth=1, Name='${1:窗口名称}')",
      "# 实例化绑定 ${1:窗口名称} 窗口",
      "# searchDepth=1 限制搜索只在直接子控件层级(即第一层子控件); =2 搜索包括子级及其下一级(孙子级)控件; 以此类推",
      "# 不设置 searchDepth 则递归地遍历搜索整个桌面控件,直到找到匹配项或到达控件树的末端"
    ],
    "description": ["uiautomation.WindowControl 实例化绑定窗口"]
  },
  "WindowControl.SwitchToThisWindow 切换到绑定的窗口": {
    "prefix": [".SwitchToThisWindow -WindowControl"],
    "body": [".SwitchToThisWindow()", "# 切换到绑定的窗口"],
    "description": ["WindowControl.SwitchToThisWindow 切换到绑定的窗口"]
  },
  "WindowControl.GetChildren 获取窗口内所有控件": {
    "prefix": [
      "GetChildren =WindowControl",
      "get_windows_control =WindowControl"
    ],
    "body": [
      "def get_windows_control(windows):",
      "    \"\"\"",
      "    获取窗口内所有控件",
      "    \"\"\"",
      "    def enum_child_windows(parent):",
      "        for child in parent.GetChildren():",
      "            print(child)",
      "            enum_child_windows(child)",
      "    enum_child_windows(windows)",
      ""
    ],
    "description": ["WindowControl.GetChildren 获取窗口内所有控件"]
  }
}
